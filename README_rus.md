# AI Geo Quest

AI Geo Quest — веб-приложение на Go, которое генерирует интерактивные городские квесты с помощью ИИ на основе географического положения пользователя.

Пользователь выбирает точку на карте или передаёт свою текущую геолокацию, после чего система анализирует окружение (тип местности, объекты рядом) и создаёт тематический квест: приключенческий, исторический или исследовательский.

---

## Возможности (MVP)

* Генерация текстовых квестов по координатам
* Использование карт и геоданных (OpenStreetMap)
* Интеграция LLM для генерации заданий
* Веб-интерфейс с картой
* Без регистрации и хранения данных

---

## Архитектура

Приложение построено по принципам разделения ответственности и приближено к clean architecture.

### Основные компоненты:

* **Frontend** — карта, выбор координат, отображение квеста
* **Backend (Go)** — REST API и бизнес-логика
* **Maps API** — получение информации о местности
* **LLM** — генерация квестов

```
Browser
  ↓
Frontend (HTML / JS / Map)
  ↓ HTTP
Go Backend (API)
  ↓
Maps API (OSM)
  ↓
LLM (AI)
```

---

## Структура проекта

```
/ai-geo-quest
│
├── cmd/
│   └── server/
│       └── main.go          # Точка входа
│
├── internal/
│   ├── api/                 # HTTP слой
│   │   ├── handler/
│   │   │   └── quest.go     # Обработчики API
│   │   └── router.go        # Роутинг
│   │
│   ├── domain/              # Доменные модели
│   │   └── quest.go
│   │
│   ├── service/             # Бизнес-логика (use cases)
│   │   └── quest_generator.go
│   │
│   ├── maps/                # Интеграция с картами (OSM)
│   │   └── osm_client.go
│   │
│   └── ai/                  # Интеграция с LLM
│       └── client.go
│
├── web/                     # Frontend
│   ├── index.html
│   ├── app.js
│   └── style.css
│
├── configs/
│   └── config.yaml
│
├── go.mod
└── README.md
```

---

## Domain-модель

Ключевая бизнес-сущность — **Quest**.

* Quest описывает сценарий квеста
* Состоит из вводного текста и набора заданий
* Не зависит от HTTP, карт или ИИ

Domain-слой используется сервисами и API, но не знает о внешних интеграциях.

---

## API (план)

### Генерация квеста

`POST /api/quest/generate`

**Request:**

```json
{
  "lat": 59.9343,
  "lng": 30.3351,
  "style": "adventure",
  "difficulty": "easy"
}
```

**Response:**

```json
{
  "title": "Тайна старых дворов",
  "intro": "...",
  "tasks": [
    {
      "id": 1,
      "text": "Найди старую арку рядом",
      "hint": "Она старше 100 лет"
    }
  ]
}
```

---

## Технологии

* Go 1.22+
* net/http / chi
* OpenStreetMap (Nominatim)
* LLM (OpenAI или аналог)
* HTML, CSS, JavaScript
* Leaflet.js

---

## Ограничения и безопасность

Квесты:

* не требуют входа в здания
* не затрагивают частную собственность
* не предполагают опасных действий
* подходят для дневного времени

Эти ограничения учитываются при генерации через ИИ.

---

## Планы развития

* Сохранение и история квестов
* Мультишаговые квесты с прогрессом
* Поддержка нескольких языков
* Авторизация пользователей
* Мобильная версия

---

## Статус проекта

Проект находится в стадии проектирования и активной разработки.
